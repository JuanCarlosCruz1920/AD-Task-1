{
    "name": "juancarloscruz/ad-task-1-main",
    "description": "A PHP project for calendar and release management",
    "type": "project",
    "require": {
        "php": "^7.4",
        "vlucas/phpdotenv": "^5.5",
        "symfony/var-dumper": "^6.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "scripts": {
        "post-install-cmd": [
            "@php -r \"file_exists('.env') || copy('.env.example', '.env');\""
        ]
    }
}
